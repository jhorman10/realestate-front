# 🏠 Real Estate Frontend



**Una aplicación moderna de gestión de propiedades inmobiliarias construida con React 19, TypeScript y Tailwind CSS v4.**


![React](https://img.shields.io/badge/React-19.1.1-61DAFB?style=for-the-badge&logo=react&logoColor=black)

![TypeScript](https://img.shields.io/badge/TypeScript-5.8.3-3178C6?style=for-the-badge&logo=typescript&logoColor=white)

![Tailwind CSS](https://img.shields.io/badge/Tailwind_CSS-4.1.13-38B2AC?style=for-the-badge&logo=tailwind-css&logoColor=white)<div align="center">> Una aplicación moderna de gestión de propiedades inmobiliarias construida con React 19, TypeScript, y Tailwind CSS v4.> Una aplicación moderna de gestión de propiedades inmobiliarias construida con React 19, TypeScript, y Tailwind CSS v4.

![Vite](https://img.shields.io/badge/Vite-7.1.2-646CFF?style=for-the-badge&logo=vite&logoColor=white)



</div>

![React](https://img.shields.io/badge/React-19.1.1-61DAFB?style=for-the-badge&logo=react&logoColor=black)

---

![TypeScript](https://img.shields.io/badge/TypeScript-5.8.3-3178C6?style=for-the-badge&logo=typescript&logoColor=white)

## ✨ Características Principales

![Tailwind CSS](https://img.shields.io/badge/Tailwind_CSS-4.1.13-38B2AC?style=for-the-badge&logo=tailwind-css&logoColor=white)![React](https://img.shields.io/badge/React-19.1.1-61DAFB?style=for-the-badge&logo=react&logoColor=black)![React](https://img.shields.io/badge/React-19.1.1-61DAFB?style=for-the-badge&logo=react&logoColor=black)

- 🏗️ **Arquitectura Moderna**: React 19 + TypeScript + Vite

- 🎨 **UI/UX Premium**: Tailwind CSS v4 con dark mode completo y funcional  ![Vite](https://img.shields.io/badge/Vite-7.1.2-646CFF?style=for-the-badge&logo=vite&logoColor=white)

- 🔄 **Estado Global**: Redux Toolkit + React Query para gestión óptima

- 📱 **Responsive Design**: Mobile-first con transiciones fluidas![TypeScript](https://img.shields.io/badge/TypeScript-5.8.3-3178C6?style=for-the-badge&logo=typescript&logoColor=white)![TypeScript](https://img.shields.io/badge/TypeScript-5.8.3-3178C6?style=for-the-badge&logo=typescript&logoColor=white)

- 🔐 **Validación Robusta**: React Hook Form + Zod schemas

- 🚀 **Performance**: Cache inteligente y optimizaciones avanzadas</div>

- 🌙 **Dark Mode**: Tema claro/oscuro con persistencia y transiciones suaves

- ✨ **Transiciones**: Animaciones suaves en toda la aplicación![Tailwind CSS](https://img.shields.io/badge/Tailwind_CSS-4.1.13-38B2AC?style=for-the-badge&logo=tailwind-css&logoColor=white)![Tailwind CSS](https://img.shields.io/badge/Tailwind_CSS-4.1.13-38B2AC?style=for-the-badge&logo=tailwind-css&logoColor=white)

- 📋 **Formularios Inteligentes**: Validación en tiempo real con mensajes personalizados

---

---

![Vite](https://img.shields.io/badge/Vite-7.1.2-646CFF?style=for-the-badge&logo=vite&logoColor=white)![Vite](https://img.shields.io/badge/Vite-7.1.2-646CFF?style=for-the-badge&logo=vite&logoColor=white)

## 🚀 Inicio Rápido

## ✨ Características Principales

### 📋 Prerrequisitos



```bash

node >= 18.0.0- 🏗️ **Arquitectura Moderna**: React 19 + TypeScript + Vite

yarn >= 1.22.0

```- 🎨 **UI/UX Premium**: Tailwind CSS v4 con dark mode completo y funcional  ## ✨ Características Principales## ✨ Características Principales



### ⚡ Instalación- 🔄 **Estado Global**: Redux Toolkit + React Query para gestión óptima



1. **Clonar el repositorio**- 📱 **Responsive Design**: Mobile-first con transiciones fluidas

```bash

git clone https://github.com/jhorman10/realestate-front.git- 🔐 **Validación Robusta**: React Hook Form + Zod schemas

cd realestate-frontend

```- 🚀 **Performance**: Cache inteligente y optimizaciones avanzadas- 🏗️ **Arquitectura Moderna**: React 19 + TypeScript + Vite- 🏗️ **Arquitectura Moderna**: React 19 + TypeScript + Vite



2. **Instalar dependencias**- 🌙 **Dark Mode**: Tema claro/oscuro con persistencia y transiciones suaves

```bash

yarn install- ✨ **Transiciones**: Animaciones suaves en toda la aplicación- 🎨 **UI/UX Premium**: Tailwind CSS v4 con dark mode completo- 🎨 **UI/UX Premium**: Tailwind CSS v4 con dark mode completo

```

- 📋 **Formularios Inteligentes**: Validación en tiempo real con mensajes personalizados

3. **Configurar variables de entorno**

```bash- 🔄 **Estado Global**: Redux Toolkit + React Query para gestión óptima- 🔄 **Estado Global**: Redux Toolkit + React Query para gestión óptima

cp .env.example .env

```---



Editar `.env` con tu configuración:- 📱 **Responsive Design**: Mobile-first con transiciones fluidas- 📱 **Responsive Design**: Mobile-first con transiciones fluidas

```bash

# Backend API Configuration## 🚀 Inicio Rápido

VITE_API_BASE_URL=http://localhost:5000

VITE_API_TIMEOUT=10000- 🔐 **Validación Robusta**: React Hook Form + Zod schemas- 🔐 **Validación Robusta**: React Hook Form + Zod schemas



# Development Configuration  ### 📋 Prerrequisitos

VITE_DEV_MODE=true

VITE_APP_NAME="Real Estate App"- 🚀 **Performance**: Cache inteligente y optimizaciones avanzadas- 🚀 **Performance**: Cache inteligente y optimizaciones avanzadas

VITE_APP_VERSION="1.0.0"

``````bash



4. **Iniciar el servidor de desarrollo**node >= 18.0.0- 🌙 **Dark Mode**: Tema claro/oscuro con persistencia- 🌙 **Dark Mode**: Tema claro/oscuro con persistencia

```bash

yarn devyarn >= 1.22.0

```

```- ✨ **Transiciones**: Animaciones suaves en toda la aplicación- ✨ **Transiciones**: Animaciones suaves en toda la aplicación

🌐 **La aplicación estará disponible en:** `http://localhost:5173`



---

### ⚡ Instalación

## 📁 Estructura del Proyecto



```

src/1. **Clonar el repositorio**## 🚀 Inicio Rápido## 🚀 Inicio Rápido

├── components/           # Componentes reutilizables

│   ├── forms/           # Formularios especializados```bash

│   │   ├── PropertyForm.tsx        # Formulario de creación

│   │   └── EditPropertyForm.tsx    # Formulario de edicióngit clone https://github.com/jhorman10/realestate-front.git

│   ├── layout/          # Componentes de layout

│   │   ├── Header.tsx              # Cabecera principalcd realestate-frontend

│   │   ├── Layout.tsx              # Layout principal  

│   │   └── Navbar.tsx              # Navegación```### Prerrequisitos### Prerrequisitos

│   └── ui/              # Componentes UI básicos

│       ├── LoadingSpinner.tsx      # Spinner de loading

│       ├── Notifications.tsx       # Sistema de notificaciones

│       └── PageTransition.tsx      # Transiciones de páginas2. **Instalar dependencias**

├── constants/           # Constantes y configuración

│   └── routes.ts        # Rutas tipadas y helpers```bash

├── context/            # React Context providers

│   └── ThemeContext.tsx # Gestión de tema claro/oscuroyarn install```bash```bash

├── hooks/              # Custom hooks

│   ├── useProperties.ts        # Hook legacy de propiedades```

│   └── usePropertyApi.ts       # Hooks de API con React Query

├── pages/              # Páginas principalesnode >= 18.0.0node >= 18.0.0

│   ├── HomePage.tsx               # Página de inicio

│   ├── NotFoundPage.tsx          # Página 4043. **Configurar variables de entorno**

│   └── properties/               # Módulo de propiedades

│       ├── CreatePropertyPage.tsx    # Crear propiedad```bashyarn >= 1.22.0yarn >= 1.22.0

│       ├── EditPropertyPage.tsx      # Editar propiedad

│       ├── PropertyDetailPage.tsx    # Detalle de propiedadcp .env.example .env

│       └── PropertyListPage.tsx      # Lista de propiedades

├── router/             # Configuración de rutas`````````

│   └── routes.tsx      # Definición de rutas con transiciones

├── schemas/            # Validación con Zod

│   └── propertySchemas.ts # Schemas de validación

├── services/           # Servicios de APIEditar `.env` con tu configuración:

│   ├── api.ts                    # Cliente HTTP base

│   ├── propertyApiService.ts     # Servicio de propiedades```bash

│   └── propertyService.ts        # Servicio legacy

├── store/              # Redux Toolkit store# Backend API Configuration### Instalación### Instalación

│   ├── index.ts                  # Configuración del store

│   └── slices/                   # Redux slicesVITE_API_BASE_URL=http://localhost:5000

│       ├── propertySlice.ts          # Estado de propiedades

│       └── uiSlice.ts                # Estado de UIVITE_API_TIMEOUT=10000

├── types/              # Definiciones TypeScript

│   └── property.ts     # Tipos de propiedades

└── utils/              # Utilidades y helpers

```# Development Configuration  1. **Clonar el repositorio**1. **Clonar el repositorio**



---VITE_DEV_MODE=true



## 🎯 Páginas y FuncionalidadesVITE_APP_NAME="Real Estate App"```bash```bash



| 🛣️ Ruta | 📄 Página | 📝 Descripción |VITE_APP_VERSION="1.0.0"

|---------|-----------|----------------|

| `/` | **HomePage** | Dashboard principal con hero section y estadísticas |```git clone <repository-url>git clone <repository-url>

| `/properties` | **PropertyListPage** | Lista completa con filtros avanzados y paginación |

| `/properties/create` | **CreatePropertyPage** | Formulario de creación con validación en tiempo real |

| `/properties/:id` | **PropertyDetailPage** | Vista detallada con información completa |

| `/properties/:id/edit` | **EditPropertyPage** | Formulario de edición con datos precargados |4. **Iniciar el servidor de desarrollo**cd realestate-frontendcd realestate-frontend

| `*` | **NotFoundPage** | Página 404 con navegación de regreso |

```bash

---

yarn dev``````

## 🔧 Scripts Disponibles

```

```bash

# 🚀 Desarrollo

yarn dev              # Inicia servidor de desarrollo (puerto 5173)

🌐 **La aplicación estará disponible en:** `http://localhost:5173`

# 🏗️ Construcción

yarn build            # Construye para producción2. **Instalar dependencias**2. **Instalar dependencias**

yarn preview          # Preview de la build de producción

---

# 🔍 Calidad de código

yarn lint             # Ejecuta ESLint```bash```bash

```

## 📁 Estructura del Proyecto

---

yarn installyarn install

## 🛠️ Stack Tecnológico

```

### 🎯 Core Framework

- **React 19.1.1** - Biblioteca principal con las últimas característicassrc/```

- **TypeScript 5.8.3** - Tipado estático para mayor robustez  

- **Vite 7.1.2** - Build tool ultra-rápido y optimizado├── components/           # Componentes reutilizables



### 🎨 UI & Styling│   ├── forms/           # Formularios especializados```

- **Tailwind CSS 4.1.13** - Framework CSS utility-first con dark mode nativo

- **Flowbite React 0.12.9** - Componentes UI predefinidos│   │   ├── PropertyForm.tsx        # Formulario de creación

- **Lucide React 0.544.0** - Iconos modernos y consistentes

- **React Icons 5.5.0** - Conjunto completo de iconos│   │   └── EditPropertyForm.tsx    # Formulario de edición3. **Configurar variables de entorno**

- **Google Fonts (Inter)** - Tipografía profesional

│   ├── layout/          # Componentes de layout

### 🔄 Estado & Datos

- **Redux Toolkit 2.9.0** - Gestión de estado global simplificada│   │   ├── Header.tsx              # Cabecera principal```bashYou can also install [eslint-plugin-react-x](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-x) and [eslint-plugin-react-dom](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-dom) for React-specific lint rules:

- **React Query 5.89.0** - Cache inteligente y sincronización de datos  

- **React Query DevTools** - Herramientas de desarrollo para debugging│   │   ├── Layout.tsx              # Layout principal  



### 📝 Formularios & Validación│   │   └── Navbar.tsx              # Navegacióncp .env.example .env

- **React Hook Form 7.62.0** - Manejo eficiente de formularios

- **Zod 4.1.8** - Validación de esquemas TypeScript-first│   └── ui/              # Componentes UI básicos

- **@hookform/resolvers 5.2.2** - Integración RHF + Zod

│       ├── LoadingSpinner.tsx      # Spinner de loading```3. **Configurar variables de entorno**

### 🌐 HTTP & API

- **Axios 1.12.2** - Cliente HTTP con interceptors│       ├── Notifications.tsx       # Sistema de notificaciones

- **Custom API Layer** - Servicios tipados y centralizados

│       └── PageTransition.tsx      # Transiciones de páginas

### 🧭 Routing & Navigation  

- **React Router DOM 7.9.1** - Routing declarativo con lazy loading├── constants/           # Constantes y configuración

- **Custom Page Transitions** - Transiciones fluidas entre páginas

│   └── routes.ts        # Rutas tipadas y helpersEditar `.env` con tu configuración:```bash```js

### 🔔 UX Enhancements

- **React Hot Toast 2.6.0** - Sistema de notificaciones elegante├── context/            # React Context providers

- **Custom Loading States** - Estados de carga con animaciones

│   └── ThemeContext.tsx # Gestión de tema claro/oscuro```bash

---

├── hooks/              # Custom hooks

## 🎨 Sistema de Temas

│   ├── useProperties.ts        # Hook legacy de propiedades# Backend API Configurationcp .env.example .env// eslint.config.js

### 🌙 Dark Mode Implementación

│   └── usePropertyApi.ts       # Hooks de API con React Query

El proyecto incluye un **sistema de dark mode completamente funcional** con:

├── pages/              # Páginas principalesVITE_API_BASE_URL=http://localhost:5000

- ✅ **Tailwind CSS v4** con custom variant `@custom-variant dark (&:is(.dark *))`

- ✅ **Context API** para gestión global del tema│   ├── HomePage.tsx               # Página de inicio

- ✅ **LocalStorage** para persistencia de preferencias

- ✅ **Detección automática** del tema del sistema│   ├── NotFoundPage.tsx          # Página 404```import reactX from 'eslint-plugin-react-x'

- ✅ **Transiciones suaves** entre temas (0.4s ease)

- ✅ **Fallbacks CSS manuales** para garantizar funcionalidad│   └── properties/               # Módulo de propiedades



### 🎨 Paleta de Colores│       ├── CreatePropertyPage.tsx    # Crear propiedad# Development Configuration



```css│       ├── EditPropertyPage.tsx      # Editar propiedad

/* 🌞 Light Mode */

--primary: #3b82f6      /* Blue 500 */│       ├── PropertyDetailPage.tsx    # Detalle de propiedadVITE_DEV_MODE=trueimport reactDom from 'eslint-plugin-react-dom'

--secondary: #64748b    /* Slate 500 */

--accent: #10b981       /* Emerald 500 */│       └── PropertyListPage.tsx      # Lista de propiedades

--background: #ffffff   /* White */

--foreground: #0f172a   /* Slate 900 */├── router/             # Configuración de rutas```



/* 🌙 Dark Mode */  │   └── routes.tsx      # Definición de rutas con transiciones

--primary: #60a5fa      /* Blue 400 */

--secondary: #94a3b8    /* Slate 400 */├── schemas/            # Validación con ZodEditar `.env` con tu configuración:

--accent: #34d399       /* Emerald 400 */

--background: #111827   /* Gray 900 */│   └── propertySchemas.ts # Schemas de validación

--foreground: #f8fafc   /* Slate 50 */

```├── services/           # Servicios de API4. **Iniciar el servidor de desarrollo**



### 💡 Uso del Theme Context│   ├── api.ts                    # Cliente HTTP base



```tsx│   ├── propertyApiService.ts     # Servicio de propiedades```bash```bashexport default tseslint.config([

import { useTheme } from '../context/ThemeContext';

│   └── propertyService.ts        # Servicio legacy

const Component = () => {

  const { theme, toggleTheme } = useTheme();├── store/              # Redux Toolkit storeyarn dev

  

  return (│   ├── index.ts                  # Configuración del store

    <button 

      onClick={toggleTheme}│   └── slices/                   # Redux slices```# Backend API Configuration  globalIgnores(['dist']),

      className="bg-white dark:bg-gray-800 text-gray-900 dark:text-white 

                 transition-colors duration-300"│       ├── propertySlice.ts          # Estado de propiedades

    >

      {theme === 'light' ? '🌙' : '☀️'} Toggle Theme│       └── uiSlice.ts                # Estado de UI

    </button>

  );├── types/              # Definiciones TypeScript

};

```│   └── property.ts     # Tipos de propiedadesLa aplicación estará disponible en `http://localhost:5173`VITE_API_BASE_URL=http://localhost:5000  {



---└── utils/              # Utilidades y helpers



## ✨ Sistema de Transiciones```



### 🎬 Componentes de Transición



El proyecto incluye un sistema completo de transiciones:---## 📁 Estructura del ProyectoVITE_API_TIMEOUT=10000    files: ['**/*.{ts,tsx}'],



**📦 Componentes Disponibles:**

- `PageTransition` - Transiciones entre páginas

- `LoadingTransition` - Estados de loading con overlay## 🎯 Páginas y Funcionalidades

- `PageWrapper` - Wrapper automático para todas las páginas



**🎨 Tipos de Animación:**

- **Fade In**: Entrada suave con opacidad (0.5s)| 🛣️ Ruta | 📄 Página | 📝 Descripción |```VITE_API_RETRY_ATTEMPTS=3    extends: [

- **Slide In**: Deslizamiento lateral (0.4s)  

- **Scale In**: Escalado desde el centro (0.3s)|---------|-----------|----------------|

- **Pulse**: Animación de loading infinita

| `/` | **HomePage** | Dashboard principal con hero section y estadísticas |src/

### 🔧 CSS Global de Transiciones

| `/properties` | **PropertyListPage** | Lista completa con filtros avanzados y paginación |

```css

/* Transiciones globales suaves */| `/properties/create` | **CreatePropertyPage** | Formulario de creación con validación en tiempo real |├── components/          # Componentes reutilizables      // Other configs...

* {

  transition: background-color 0.3s ease, | `/properties/:id` | **PropertyDetailPage** | Vista detallada con información completa |

              color 0.3s ease, 

              border-color 0.3s ease, | `/properties/:id/edit` | **EditPropertyPage** | Formulario de edición con datos precargados |│   ├── forms/          # Formularios especializados

              opacity 0.3s ease, 

              transform 0.3s ease;| `*` | **NotFoundPage** | Página 404 con navegación de regreso |

}

│   ├── layout/         # Componentes de layout# Application Configuration      // Enable lint rules for React

/* Clases de utilidad */

.transition-page   { animation: fadeIn 0.5s ease-out; }---

.transition-slide  { animation: slideIn 0.4s ease-out; }

.transition-scale  { animation: scaleIn 0.3s ease-out; }│   └── ui/             # Componentes UI básicos

.loading-pulse     { animation: pulse 1.5s ease-in-out infinite; }

```## 🔧 Scripts Disponibles



### 🚀 Uso en Componentes├── hooks/              # Custom hooksVITE_APP_NAME=Real Estate App      reactX.configs['recommended-typescript'],



```tsx```bash

import { PageWrapper, LoadingTransition } from '../components/ui/PageTransition';

# 🚀 Desarrollo├── pages/              # Páginas de la aplicación

const MyPage = () => (

  <PageWrapper>yarn dev              # Inicia servidor de desarrollo (puerto 5173)

    <div className="transition-scale">

      <LoadingTransition isLoading={loading}>├── router/             # Configuración de rutasVITE_APP_VERSION=1.0.0      // Enable lint rules for React DOM

        {/* Contenido con transiciones automáticas */}

      </LoadingTransition># 🏗️ Construcción

    </div>

  </PageWrapper>yarn build            # Construye para producción├── services/           # Servicios y APIs

);

```yarn preview          # Preview de la build de producción



---├── store/              # Estado global (Redux)```      reactDom.configs.recommended,



## 🔄 Gestión de Estado# 🔍 Calidad de código



### 🏪 Redux Toolkit + React Queryyarn lint             # Ejecuta ESLint├── types/              # Definiciones TypeScript



**🎯 Arquitectura Híbrida:**```

- **Redux Toolkit** para estado de UI (tema, notificaciones, sidebar)

- **React Query** para datos del servidor (propiedades, usuarios)└── utils/              # Utilidades y helpers    ],



```typescript---

// 🏪 Store Structure

interface RootState {```

  properties: PropertyState;  // Lista, filtros, selección

  ui: UIState;               // Tema, notificaciones, loading## 🛠️ Stack Tecnológico

}

```4. **Iniciar servidor de desarrollo**    languageOptions: {



### ⚡ React Query Configuration### 🎯 Core Framework



```typescript- **React 19.1.1** - Biblioteca principal con las últimas características## 🎯 Páginas Principales

// ⚙️ Configuración optimizada

const queryClient = new QueryClient({- **TypeScript 5.8.3** - Tipado estático para mayor robustez  

  defaultOptions: {

    queries: {- **Vite 7.1.2** - Build tool ultra-rápido y optimizado```bash      parserOptions: {

      staleTime: 5 * 60 * 1000,     // 5 minutos

      gcTime: 10 * 60 * 1000,       // 10 minutos  

      refetchOnWindowFocus: false,   // No refetch automático

      retry: 3,                      // 3 reintentos### 🎨 UI & Styling- **🏠 Home**: Dashboard principal con estadísticas

    }

  }- **Tailwind CSS 4.1.13** - Framework CSS utility-first con dark mode nativo

});

```- **Flowbite React 0.12.9** - Componentes UI predefinidos- **📋 Lista de Propiedades**: Vista completa con filtros avanzadosnpm run dev        project: ['./tsconfig.node.json', './tsconfig.app.json'],



### 🎣 Custom Hooks Ejemplo- **Lucide React 0.544.0** - Iconos modernos y consistentes



```tsx- **React Icons 5.5.0** - Conjunto completo de iconos- **➕ Crear Propiedad**: Formulario completo de registro

// 🎣 usePropertyApi.ts

export const useProperties = () => {- **Google Fonts (Inter)** - Tipografía profesional

  return useQuery({

    queryKey: ['properties'],- **✏️ Editar Propiedad**: Actualización de propiedades existentes```        tsconfigRootDir: import.meta.dirname,

    queryFn: propertyApiService.getProperties,

    staleTime: 5 * 60 * 1000,### 🔄 Estado & Datos

  });

};- **Redux Toolkit 2.9.0** - Gestión de estado global simplificada- **👁️ Detalle de Propiedad**: Vista completa con información detallada



export const useCreateProperty = () => {- **React Query 5.89.0** - Cache inteligente y sincronización de datos  

  const queryClient = useQueryClient();

  - **React Query DevTools** - Herramientas de desarrollo para debugging      },

  return useMutation({

    mutationFn: propertyApiService.createProperty,

    onSuccess: () => {

      queryClient.invalidateQueries({ queryKey: ['properties'] });### 📝 Formularios & Validación## 🔧 Scripts Disponibles

      toast.success('Propiedad creada exitosamente!');

    }- **React Hook Form 7.62.0** - Manejo eficiente de formularios

  });

};- **Zod 4.1.8** - Validación de esquemas TypeScript-first5. **Abrir en el navegador**      // other options...

```

- **@hookform/resolvers 5.2.2** - Integración RHF + Zod

---

```bash

## 📝 Formularios y Validación

### 🌐 HTTP & API

### 🔒 React Hook Form + Zod

- **Axios 1.12.2** - Cliente HTTP con interceptors# Desarrollo```    },

**Sistema robusto de validación con:**

- ✅ Validación en tiempo real- **Custom API Layer** - Servicios tipados y centralizados

- ✅ Mensajes de error personalizados  

- ✅ Tipos automáticos desde esquemasyarn dev              # Inicia servidor de desarrollo

- ✅ Optimización de re-renders

### 🧭 Routing & Navigation  

### 📋 Esquemas Disponibles

- **React Router DOM 7.9.1** - Routing declarativo con lazy loadinghttp://localhost:5173  },

```typescript

// 📄 propertySchemas.ts- **Custom Page Transitions** - Transiciones fluidas entre páginas

export const CreatePropertyRequestSchema = z.object({

  title: z.string()# Construcción

    .min(1, "El título es requerido")

    .max(100, "El título no puede exceder 100 caracteres"),### 🔔 UX Enhancements

  

  price: z.number()- **React Hot Toast 2.6.0** - Sistema de notificaciones eleganteyarn build            # Construye para producción```])

    .positive("El precio debe ser positivo")

    .max(1000000000, "Precio máximo excedido"),- **Custom Loading States** - Estados de carga con animaciones

    

  type: z.enum(["HOUSE", "APARTMENT", "LAND"], {yarn preview          # Preview de la build de producción

    errorMap: () => ({ message: "Selecciona un tipo válido" })

  }),---

  

  description: z.string()```

    .min(10, "La descripción debe tener al menos 10 caracteres")

    .max(500, "La descripción no puede exceder 500 caracteres"),## 🎨 Sistema de Temas

});

```# Calidad de código



### 🎯 Uso en Componentes### 🌙 Dark Mode Implementación



```tsxyarn lint             # Ejecuta ESLint## 📁 Estructura del Proyecto

const PropertyForm = () => {

  const {El proyecto incluye un **sistema de dark mode completamente funcional** con:

    register,

    handleSubmit,yarn lint:fix         # Corrige errores de ESLint automáticamente

    formState: { errors, isSubmitting },

    watch- ✅ **Tailwind CSS v4** con custom variant `@custom-variant dark (&:is(.dark *))`

  } = useForm({

    resolver: zodResolver(CreatePropertyRequestSchema),- ✅ **Context API** para gestión global del temayarn type-check       # Verifica tipos TypeScript```

    mode: 'onChange' // Validación en tiempo real

  });- ✅ **LocalStorage** para persistencia de preferencias



  const onSubmit = async (data: CreatePropertyRequest) => {- ✅ **Detección automática** del tema del sistemasrc/

    // Los datos están completamente validados

    await createProperty(data);- ✅ **Transiciones suaves** entre temas (0.4s ease)

  };

- ✅ **Fallbacks CSS manuales** para garantizar funcionalidad# Mantenimiento├── components/           # Componentes reutilizables

  return (

    <form onSubmit={handleSubmit(onSubmit)} className="space-y-6">

      <input

        {...register('title')}### 🎨 Paleta de Coloresyarn clean            # Limpia archivos generados│   ├── forms/           # Formularios (PropertyForm, EditPropertyForm)

        className="form-transition focus:scale-105"

      />

      {errors.title && (

        <p className="text-red-500 text-sm">{errors.title.message}</p>```css```│   ├── layout/          # Layout components (Navbar, Header, Layout)

      )}

    </form>/* 🌞 Light Mode */

  );

};--primary: #3b82f6      /* Blue 500 */│   └── ui/              # UI components (PageTransition, LoadingSpinner)

```

--secondary: #64748b    /* Slate 500 */

---

--accent: #10b981       /* Emerald 500 */## 🛠️ Tecnologías Utilizadas├── constants/           # Constantes y configuración

## 📱 Responsive Design

--background: #ffffff   /* White */

### 📐 Breakpoints Strategy

--foreground: #0f172a   /* Slate 900 */│   └── routes.ts        # Rutas tipadas y helpers

```css

/* 📱 Mobile First Approach */

/* Default: < 640px (Mobile) */

.grid-cols-1/* 🌙 Dark Mode */  ### Core├── context/            # React Context providers



/* 📲 Small: ≥ 640px (Large Mobile) */--primary: #60a5fa      /* Blue 400 */

.sm:grid-cols-2

--secondary: #94a3b8    /* Slate 400 */- **React 19**: Biblioteca principal con las últimas características│   └── ThemeContext.tsx # Gestión de tema claro/oscuro

/* 💻 Medium: ≥ 768px (Tablet) */

.md:grid-cols-3--accent: #34d399       /* Emerald 400 */



/* 🖥️ Large: ≥ 1024px (Desktop) */--background: #111827   /* Gray 900 */- **TypeScript 5.8**: Tipado estático para mayor robustez├── hooks/              # Custom hooks

.lg:grid-cols-4

--foreground: #f8fafc   /* Slate 50 */

/* 🖥️ XL: ≥ 1280px (Large Desktop) */

.xl:grid-cols-5```- **Vite 7**: Build tool ultra-rápido y optimizado│   ├── usePropertyApi.ts   # Hooks de API con React Query

```



### 🎯 Características Responsive

### 💡 Uso del Theme Context│   ├── useProperties.ts    # Hooks legacy (compatibilidad)

- **📱 Navegación móvil** con menú hamburguesa animado

- **🔄 Grid adaptativo** para tarjetas de propiedades

- **📋 Formularios apilados** en dispositivos móviles

- **👆 Touch targets optimizados** (min 44px)```tsx### UI & Estilo│   └── useTransitions.ts   # Hooks para transiciones

- **🖼️ Imágenes responsivas** con lazy loading

- **📊 Tablas scrolleables** en pantallas pequeñasimport { useTheme } from '../context/ThemeContext';



---- **Tailwind CSS v4**: Framework CSS utility-first├── pages/              # Páginas principales



## 🚀 Optimizaciones de Performanceconst Component = () => {



### ⚡ Implementadas  const { theme, toggleTheme } = useTheme();- **Custom Transitions**: Sistema de transiciones personalizado│   ├── HomePage.tsx           # Página de inicio



- **📦 Code Splitting** automático con Vite  

- **🗄️ Cache Strategy** inteligente (5min stale, 10min gc)

- **🔄 Optimistic Updates** en mutaciones  return (- **Dark Mode**: Implementación completa con persistencia│   ├── NotFoundPage.tsx       # Página 404

- **🌳 Tree Shaking** automático para CSS y JS

- **🖼️ Lazy Loading** de componentes pesados    <button 

- **📱 Service Worker** ready (PWA)

      onClick={toggleTheme}│   └── properties/            # Páginas de propiedades

### 📊 Métricas de Build

      className="bg-white dark:bg-gray-800 text-gray-900 dark:text-white 

```bash

📦 Build Output:                 transition-colors duration-300"### Estado y Datos│       ├── PropertyListPage.tsx    # Lista de propiedades

├── CSS: 38.42 kB (gzipped: 7.43 kB)

├── JavaScript: 505.98 kB (gzipped: 157.86 kB)      >

└── Build Time: ~1.75s

      {theme === 'light' ? '🌙' : '☀️'} Toggle Theme- **Redux Toolkit**: Gestión de estado global simplificada│       ├── PropertyDetailPage.tsx  # Detalle de propiedad

🚀 Performance Score:

├── First Contentful Paint: <1.5s    </button>

├── Largest Contentful Paint: <2.5s

└── Cumulative Layout Shift: <0.1  );- **React Query**: Cache inteligente y sincronización de datos│       ├── CreatePropertyPage.tsx  # Crear propiedad

```

};

---

```- **React Hook Form**: Manejo eficiente de formularios│       └── EditPropertyPage.tsx    # Editar propiedad

## 🐛 Troubleshooting



### ❗ Problemas Comunes

---- **Zod**: Validación de esquemas TypeScript-first├── router/             # Configuración de rutas

**1. 🚫 Error de CORS**

```bash

# Verificar que el backend esté en puerto 5000

curl http://localhost:5000/health## ✨ Sistema de Transiciones│   └── routes.tsx      # Definición de rutas con transiciones



# Actualizar variables de entorno

echo "VITE_API_BASE_URL=http://localhost:5000" > .env

```### 🎬 Componentes de Transición### Routing y Navegación├── schemas/            # Validación con Zod



**2. 🌙 Dark Mode no funciona**

```bash

# Verificar que el CSS tenga la línea críticaEl proyecto incluye un sistema completo de transiciones:- **React Router v7**: Routing declarativo con lazy loading│   └── propertySchemas.ts # Schemas para propiedades

grep "@custom-variant dark" src/index.css



# Comprobar clase en HTML

# DevTools → Elements → <html class="dark">**📦 Componentes Disponibles:**- **Page Transitions**: Transiciones fluidas entre páginas├── services/           # Servicios de API

```

- `PageTransition` - Transiciones entre páginas

**3. 🛣️ Rutas no funcionan en producción**

```bash- `LoadingTransition` - Estados de loading con overlay│   ├── api.ts                # Cliente HTTP base

# Configurar redirects en Vercel/Netlify

echo '/*    /index.html   200' > public/_redirects- `PageWrapper` - Wrapper automático para todas las páginas

```

## 🎨 Sistema de Diseño│   ├── propertyApiService.ts # Servicio principal de propiedades

**4. 📝 TypeScript Errors**

```bash**🎨 Tipos de Animación:**

# Limpiar cache y reinstalar

rm -rf node_modules/.cache dist- **Fade In**: Entrada suave con opacidad (0.5s)│   └── propertyService.ts    # Servicio legacy

yarn install

yarn build- **Slide In**: Deslizamiento lateral (0.4s)  

```

- **Scale In**: Escalado desde el centro (0.3s)### Colores├── store/              # Redux Toolkit store

---

- **Pulse**: Animación de loading infinita

## 🚀 Deployment

```css│   ├── index.ts              # Configuración del store

### 🌐 Vercel (Recomendado)

### 🔧 CSS Global de Transiciones

```json

{/* Light Mode */│   └── slices/               # Redux slices

  "buildCommand": "yarn build",

  "outputDirectory": "dist", ```css

  "framework": "vite",

  "functions": {},/* Transiciones globales suaves */--primary: #3b82f6│       ├── propertySlice.ts  # Estado de propiedades

  "routes": [

    { "handle": "filesystem" },* {

    { "src": "/.*", "dest": "/index.html" }

  ]  transition: background-color 0.3s ease, --secondary: #64748b│       └── uiSlice.ts        # Estado de UI (tema, notificaciones)

}

```              color 0.3s ease, 



### 📦 Build Manual              border-color 0.3s ease, --accent: #10b981└── types/              # Definiciones de tipos TypeScript



```bash              opacity 0.3s ease, 

# Construir para producción

yarn build              transform 0.3s ease;--background: #ffffff    └── property.ts     # Tipos de propiedades



# Servir localmente}

yarn preview

--foreground: #0f172a```

# Verificar archivos

ls -la dist//* Clases de utilidad */

```

.transition-page   { animation: fadeIn 0.5s ease-out; }

---

.transition-slide  { animation: slideIn 0.4s ease-out; }

## 🤝 Contribución

.transition-scale  { animation: scaleIn 0.3s ease-out; }/* Dark Mode */## 🗺️ Rutas de la Aplicación

### 🔧 Setup para Desarrollo

.loading-pulse     { animation: pulse 1.5s ease-in-out infinite; }

1. **Fork** el proyecto

2. **Clone** tu fork```--primary: #60a5fa

```bash

git clone https://github.com/tu-usuario/realestate-front.git

cd realestate-front

```### 🚀 Uso en Componentes--secondary: #94a3b8| Ruta | Componente | Descripción |



3. **Crear rama** para feature

```bash

git checkout -b feature/amazing-feature```tsx--accent: #34d399|------|------------|-------------|

```

import { PageWrapper, LoadingTransition } from '../components/ui/PageTransition';

4. **Hacer cambios** y commit

```bash--background: #0f172a| `/` | `HomePage` | Página de inicio con hero section |

git add .

git commit -m "✨ Add amazing feature"const MyPage = () => (

```

  <PageWrapper>--foreground: #f8fafc| `/properties` | `PropertyListPage` | Lista de propiedades con filtros |

5. **Push** y crear **Pull Request**

```bash    <div className="transition-scale">

git push origin feature/amazing-feature

```      <LoadingTransition isLoading={loading}>```| `/properties/create` | `CreatePropertyPage` | Crear nueva propiedad |



### 📝 Convenciones        {/* Contenido con transiciones automáticas */}



- **🎯 Commits**: Usar [Conventional Commits](https://www.conventionalcommits.org/)      </LoadingTransition>| `/properties/:id` | `PropertyDetailPage` | Ver detalle de propiedad |

- **📁 Archivos**: PascalCase para componentes, camelCase para utils

- **🎨 Estilos**: Tailwind classes + componentes modulares    </div>

- **📝 TypeScript**: Strict mode habilitado

  </PageWrapper>### Transiciones| `/properties/:id/edit` | `EditPropertyPage` | Editar propiedad |

---

);

## 📄 Licencia

```- **Page Transitions**: Fade, slide, scale con timing optimizado| `*` | `NotFoundPage` | Página 404 |

Este proyecto está bajo la **Licencia MIT**. Ver [LICENSE](LICENSE) para más detalles.



---

---- **Loading States**: Spinners y overlays con animaciones suaves

## 👨‍💻 Autor



**Jhorman Orozco**

- 🌐 GitHub: [@jhorman10](https://github.com/jhorman10)## 🔄 Gestión de Estado- **Hover Effects**: Transformaciones sutiles en componentes interactivos## 🎨 Sistema de Temas

- 📧 Email: jhormanorozco@gmail.com

- 🔗 LinkedIn: [jhorman-orozco](https://linkedin.com/in/jhorman-orozco)



---### 🏪 Redux Toolkit + React Query



<div align="center">



### 🏠 **Real Estate Frontend****🎯 Arquitectura Híbrida:**## 📱 Responsive DesignLa aplicación incluye soporte completo para modo claro/oscuro:

*Construido con ❤️ y las mejores prácticas de React*

- **Redux Toolkit** para estado de UI (tema, notificaciones, sidebar)

**[⭐ Star](https://github.com/jhorman10/realestate-front) • [🐛 Report Bug](https://github.com/jhorman10/realestate-front/issues) • [💡 Request Feature](https://github.com/jhorman10/realestate-front/issues)**

- **React Query** para datos del servidor (propiedades, usuarios)

</div>


```typescript- **Mobile First**: Diseño optimizado para dispositivos móviles### Configuración de Dark Mode

// 🏪 Store Structure

interface RootState {- **Breakpoints Tailwind**: `sm:` `md:` `lg:` `xl:` `2xl:`- **Activación**: Clase `.dark` en el elemento `<html>`

  properties: PropertyState;  // Lista, filtros, selección

  ui: UIState;               // Tema, notificaciones, loading- **Touch Friendly**: Elementos táctiles con tamaños apropiados- **Persistencia**: LocalStorage automático

}

```- **Performance**: Lazy loading y optimizaciones para móviles- **Transiciones**: Suaves entre temas



### ⚡ React Query Configuration- **Tailwind v4**: Custom variant `@custom-variant dark (&:is(.dark *))`



```typescript## 🔐 Validación y Formularios

// ⚙️ Configuración optimizada

const queryClient = new QueryClient({### Uso en Componentes

  defaultOptions: {

    queries: {### Esquemas Zod```tsx

      staleTime: 5 * 60 * 1000,     // 5 minutos

      gcTime: 10 * 60 * 1000,       // 10 minutos  ```typescriptimport { useTheme } from '../context/ThemeContext';

      refetchOnWindowFocus: false,   // No refetch automático

      retry: 3,                      // 3 reintentosconst propertySchema = z.object({

    }

  }  title: z.string().min(1, "Título requerido"),const Component = () => {

});

```  price: z.number().positive("Precio debe ser positivo"),  const { theme, toggleTheme } = useTheme();



### 🎣 Custom Hooks Ejemplo  type: z.enum(["HOUSE", "APARTMENT", "LAND"]),  



```tsx  // ... más validaciones  return (

// 🎣 usePropertyApi.ts

export const useProperties = () => {})    <button 

  return useQuery({

    queryKey: ['properties'],```      onClick={toggleTheme}

    queryFn: propertyApiService.getProperties,

    staleTime: 5 * 60 * 1000,      className="bg-white dark:bg-gray-800 text-gray-900 dark:text-white"

  });

};### Características    >



export const useCreateProperty = () => {- **Validación en tiempo real**: Feedback inmediato al usuario      {theme === 'light' ? '🌙' : '☀️'}

  const queryClient = useQueryClient();

  - **Mensajes personalizados**: Errores claros y útiles    </button>

  return useMutation({

    mutationFn: propertyApiService.createProperty,- **Integración TypeScript**: Tipos automáticos desde esquemas  );

    onSuccess: () => {

      queryClient.invalidateQueries({ queryKey: ['properties'] });};

      toast.success('Propiedad creada exitosamente!');

    }## 🚀 Optimizaciones de Performance```

  });

};

```

- **Lazy Loading**: Carga diferida de componentes y rutas## 🔄 Gestión de Estado

---

- **Code Splitting**: División automática del código

## 📝 Formularios y Validación

- **Image Optimization**: Carga eficiente de imágenes### Redux Toolkit + React Query

### 🔒 React Hook Form + Zod

- **Cache Strategy**: Estrategias de cache para APIs

**Sistema robusto de validación con:**

- ✅ Validación en tiempo real- **Bundle Analysis**: Análisis y optimización del bundle**Redux Toolkit** para estado de UI:

- ✅ Mensajes de error personalizados  

- ✅ Tipos automáticos desde esquemas- Tema (claro/oscuro)

- ✅ Optimización de re-renders

## 🌙 Dark Mode- Notificaciones

### 📋 Esquemas Disponibles

- Estado de sidebar

```typescript

// 📄 propertySchemas.ts### Implementación

export const CreatePropertyRequestSchema = z.object({

  title: z.string()- **Tailwind CSS v4**: Configuración nativa con custom variant**React Query** para datos del servidor:

    .min(1, "El título es requerido")

    .max(100, "El título no puede exceder 100 caracteres"),- **Context API**: Gestión global del tema- Cache inteligente (5 minutos stale time)

  

  price: z.number()- **LocalStorage**: Persistencia de preferencias- Invalidación automática

    .positive("El precio debe ser positivo")

    .max(1000000000, "Precio máximo excedido"),- **System Preference**: Detección automática del tema del sistema- Optimistic updates

    

  type: z.enum(["HOUSE", "APARTMENT", "LAND"], {- Retry logic configurable

    errorMap: () => ({ message: "Selecciona un tipo válido" })

  }),### Uso

  

  description: z.string()```tsx### Ejemplo de Uso

    .min(10, "La descripción debe tener al menos 10 caracteres")

    .max(500, "La descripción no puede exceder 500 caracteres"),const { theme, toggleTheme } = useTheme()```tsx

});

```import { useProperties, useCreateProperty } from '../hooks/usePropertyApi';



### 🎯 Uso en Componentes// Toggle manual



```tsx<button onClick={toggleTheme}>const PropertyList = () => {

const PropertyForm = () => {

  const {  {theme === 'dark' ? '☀️' : '🌙'}  const { data: properties, isLoading } = useProperties();

    register,

    handleSubmit,</button>  const createMutation = useCreateProperty();

    formState: { errors, isSubmitting },

    watch```  

  } = useForm({

    resolver: zodResolver(CreatePropertyRequestSchema),  const handleCreate = (propertyData) => {

    mode: 'onChange' // Validación en tiempo real

  });## 📦 Gestión de Estado    createMutation.mutate(propertyData, {



  const onSubmit = async (data: CreatePropertyRequest) => {      onSuccess: () => {

    // Los datos están completamente validados

    await createProperty(data);### Redux Store        // El cache se invalida automáticamente

  };

```typescript        toast.success('Property created!');

  return (

    <form onSubmit={handleSubmit(onSubmit)} className="space-y-6">// Estructura del store      }

      <input

        {...register('title')}interface RootState {    });

        className="form-transition focus:scale-105"

      />  properties: PropertyState  };

      {errors.title && (

        <p className="text-red-500 text-sm">{errors.title.message}</p>  ui: UIState};

      )}

    </form>}```

  );

};```

```

## 📋 Formularios y Validación

---

### React Query

## 📱 Responsive Design

- **Cache Management**: Cache automático con invalidación inteligente### React Hook Form + Zod

### 📐 Breakpoints Strategy

- **Background Updates**: Actualizaciones en segundo plano

```css

/* 📱 Mobile First Approach */- **Optimistic Updates**: Actualizaciones optimistas para UX fluidaValidación robusta con schemas tipados:

/* Default: < 640px (Mobile) */

.grid-cols-1



/* 📲 Small: ≥ 640px (Large Mobile) */## 🔍 API Integration```tsx

.sm:grid-cols-2

import { useForm } from 'react-hook-form';

/* 💻 Medium: ≥ 768px (Tablet) */

.md:grid-cols-3### Configuraciónimport { zodResolver } from '@hookform/resolvers/zod';



/* 🖥️ Large: ≥ 1024px (Desktop) */```typescriptimport { CreatePropertyRequestSchema } from '../schemas/propertySchemas';

.lg:grid-cols-4

// services/api.ts

/* 🖥️ XL: ≥ 1280px (Large Desktop) */

.xl:grid-cols-5const api = axios.create({const PropertyForm = () => {

```

  baseURL: import.meta.env.VITE_API_BASE_URL,  const {

### 🎯 Características Responsive

  timeout: 10000,    register,

- **📱 Navegación móvil** con menú hamburguesa animado

- **🔄 Grid adaptativo** para tarjetas de propiedades})    handleSubmit,

- **📋 Formularios apilados** en dispositivos móviles

- **👆 Touch targets optimizados** (min 44px)```    formState: { errors, isSubmitting }

- **🖼️ Imágenes responsivas** con lazy loading

- **📊 Tablas scrolleables** en pantallas pequeñas  } = useForm({



---### Endpoints    resolver: zodResolver(CreatePropertyRequestSchema)



## 🚀 Optimizaciones de Performance- `GET /api/properties` - Lista de propiedades  });



### ⚡ Implementadas- `POST /api/properties` - Crear propiedad  



- **📦 Code Splitting** automático con Vite- `PUT /api/properties/:id` - Actualizar propiedad  const onSubmit = async (data) => {

- **🗄️ Cache Strategy** inteligente (5min stale, 10min gc)

- **🔄 Optimistic Updates** en mutaciones- `DELETE /api/properties/:id` - Eliminar propiedad    // Los datos están validados automáticamente

- **🌳 Tree Shaking** automático para CSS y JS

- **🖼️ Lazy Loading** de componentes pesados  };

- **📱 Service Worker** ready (PWA)

## 🧪 Testing (Configuración futura)};

### 📊 Métricas de Build

```

```bash

📦 Build Output:```bash

├── CSS: 38.42 kB (gzipped: 7.43 kB)

├── JavaScript: 505.98 kB (gzipped: 157.86 kB)  # Testing setup recommendation### Schemas Disponibles

└── Build Time: ~1.75s

yarn add -D @testing-library/react @testing-library/jest-dom vitest- `CreatePropertyRequestSchema` - Crear propiedad

🚀 Performance Score:

├── First Contentful Paint: <1.5s```- `UpdatePropertyRequestSchema` - Actualizar propiedad

├── Largest Contentful Paint: <2.5s

└── Cumulative Layout Shift: <0.1- `PropertyFilterRequestSchema` - Filtros de búsqueda

```

## 📈 Monitoreo y Analytics (Configuración futura)- `PropertySchema` - Propiedad completa

---

- `PropertyDetailSchema` - Detalle con traces

## 🐛 Troubleshooting

```bash

### ❗ Problemas Comunes

# Analytics setup recommendation## ✨ Transiciones y Animaciones

**1. 🚫 Error de CORS**

```bashyarn add @vercel/analytics react-ga4

# Verificar que el backend esté en puerto 5000

curl http://localhost:5000/health```### Sistema de Transiciones



# Actualizar variables de entorno

echo "VITE_API_BASE_URL=http://localhost:5000" > .env

```## 🚀 Deployment**Componentes de Transición**:



**2. 🌙 Dark Mode no funciona**- `PageTransition` - Transiciones entre páginas

```bash

# Verificar que el CSS tenga la línea crítica### Build de Producción- `LoadingTransition` - Estados de loading

grep "@custom-variant dark" src/index.css

```bash- `PageWrapper` - Wrapper automático para páginas

# Comprobar clase en HTML

# DevTools → Elements → <html class="dark">yarn build

```

```**CSS Global**:

**3. 🛣️ Rutas no funcionan en producción**

```bash```css

# Configurar redirects en Vercel/Netlify

echo '/*    /index.html   200' > public/_redirects### Configuración para Vercel* {

```

```json  transition: background-color 0.3s ease, 

**4. 📝 TypeScript Errors**

```bash{              color 0.3s ease, 

# Limpiar cache y reinstalar

rm -rf node_modules/.cache dist  "buildCommand": "yarn build",              border-color 0.3s ease, 

yarn install

yarn build  "outputDirectory": "dist",              opacity 0.3s ease, 

```

  "framework": "vite"              transform 0.3s ease;

---

}}

## 🚀 Deployment

``````

### 🌐 Vercel (Recomendado)



```json

{## 🤝 Contribución**Clases de Utilidad**:

  "buildCommand": "yarn build",

  "outputDirectory": "dist", - `.transition-page` - Animación de entrada

  "framework": "vite",

  "functions": {},1. Fork el proyecto- `.transition-slide` - Deslizamiento lateral

  "routes": [

    { "handle": "filesystem" },2. Crear una rama para tu feature (`git checkout -b feature/AmazingFeature`)- `.transition-scale` - Escalado suave

    { "src": "/.*", "dest": "/index.html" }

  ]3. Commit tus cambios (`git commit -m 'Add some AmazingFeature'`)- `.loading-pulse` - Pulso de loading

}

```4. Push a la rama (`git push origin feature/AmazingFeature`)



### 📦 Build Manual5. Abrir un Pull Request### Uso de Transiciones



```bash```tsx

# Construir para producción

yarn build## 📄 Licenciaimport { PageWrapper } from '../components/ui/PageTransition';



# Servir localmente

yarn preview

Este proyecto está bajo la Licencia MIT - ver el archivo [LICENSE](LICENSE) para detalles.const MyPage = () => (

# Verificar archivos

ls -la dist/  <PageWrapper>

```

## 🙏 Agradecimientos    <div className="transition-scale">

---

      {/* Contenido con transición */}

## 🤝 Contribución

- **React Team** por React 19 y sus nuevas características    </div>

### 🔧 Setup para Desarrollo

- **Tailwind CSS** por el framework CSS v4  </PageWrapper>

1. **Fork** el proyecto

2. **Clone** tu fork- **Vite Team** por la herramienta de build ultra-rápida);

```bash

git clone https://github.com/tu-usuario/realestate-front.git- **TypeScript Team** por el tipado estático robusto```

cd realestate-front

```



3. **Crear rama** para feature---## 🛠️ Scripts Disponibles

```bash

git checkout -b feature/amazing-feature

```

**Hecho con ❤️ y las últimas tecnologías web**```bash

4. **Hacer cambios** y commit# Desarrollo

```bashnpm run dev          # Servidor de desarrollo (puerto 5173)

git add .

git commit -m "✨ Add amazing feature"# Construcción

```npm run build        # Build para producción

npm run preview      # Preview del build

5. **Push** y crear **Pull Request**

```bash# Calidad de código

git push origin feature/amazing-featurenpm run lint         # ESLint

``````



### 📝 Convenciones## 🏗️ Tecnologías Utilizadas



- **🎯 Commits**: Usar [Conventional Commits](https://www.conventionalcommits.org/)### Core Framework

- **📁 Archivos**: PascalCase para componentes, camelCase para utils- **React 19.1.1** - UI Library con las últimas características

- **🎨 Estilos**: Tailwind classes + componentes modulares- **TypeScript 5.8.3** - Tipado estático

- **📝 TypeScript**: Strict mode habilitado- **Vite 7.1.2** - Build tool ultrarrápido



---### Routing & Estado

- **React Router 7.9.1** - Routing declarativo

## 📄 Licencia- **Redux Toolkit 2.9.0** - Estado global

- **React Query 5.89.0** - Server state management

Este proyecto está bajo la **Licencia MIT**. Ver [LICENSE](LICENSE) para más detalles.

### Styling & UI

---- **Tailwind CSS 4.1.13** - Utility-first CSS

- **Lucide React** - Iconos modernos

## 👨‍💻 Autor- **React Icons** - Conjunto de iconos

- **React Hot Toast** - Notificaciones

**Jhorman Orozco**

- 🌐 GitHub: [@jhorman10](https://github.com/jhorman10)### Formularios & Validación

- 📧 Email: jhormanorozco@gmail.com- **React Hook Form 7.62.0** - Gestión de formularios

- 🔗 LinkedIn: [jhorman-orozco](https://linkedin.com/in/jhorman-orozco)- **Zod 4.1.8** - Schema validation

- **@hookform/resolvers** - Integración RHF + Zod

---

### HTTP & API

<div align="center">- **Axios 1.12.2** - Cliente HTTP

- **Custom API Layer** - Servicios tipados

### 🏠 **Real Estate Frontend**

*Construido con ❤️ y las mejores prácticas de React*## 🔧 Configuración de Desarrollo



**[⭐ Star](https://github.com/jhorman10/realestate-front) • [🐛 Report Bug](https://github.com/jhorman10/realestate-front/issues) • [💡 Request Feature](https://github.com/jhorman10/realestate-front/issues)**### ESLint

```javascript

</div>// eslint.config.js
export default [
  {
    files: ['**/*.{js,jsx,ts,tsx}'],
    rules: {
      'react-hooks/rules-of-hooks': 'error',
      'react-hooks/exhaustive-deps': 'warn',
    }
  }
];
```

### Tailwind CSS v4
```javascript
// tailwind.config.cjs
module.exports = {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  darkMode: 'class',
  theme: {
    extend: {
      fontFamily: {
        sans: ['Inter', 'ui-sans-serif', 'system-ui', 'sans-serif'],
      }
    }
  }
};
```

### PostCSS
```javascript
// postcss.config.cjs
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
};
```

## 📱 Responsive Design

### Breakpoints
- **Mobile**: `< 768px` (1 columna)
- **Tablet**: `768px - 1024px` (2 columnas)
- **Desktop**: `> 1024px` (3 columnas)

### Características Responsive
- 📱 Navegación móvil con hamburguesa
- 🔄 Grid adaptativo para tarjetas
- 📋 Formularios que se apilan en móvil
- 👆 Touch targets optimizados

## 🚀 Performance

### Optimizaciones Implementadas
- ⚡ **Code Splitting** automático con Vite
- 🗄️ **Cache Strategy** inteligente con React Query
- 🔄 **Optimistic Updates** en mutaciones
- 📦 **Bundle Optimization** con tree shaking
- 🖼️ **Lazy Loading** de componentes pesados

### Métricas de Build
- **CSS**: 38.42 kB (gzipped: 7.43 kB)
- **JavaScript**: 505.98 kB (gzipped: 157.86 kB)
- **Build Time**: ~1.75s

## 🐛 Troubleshooting

### Problemas Comunes

**1. Error de CORS**
```bash
# Verificar que el backend esté corriendo en puerto 5000
# Actualizar VITE_API_BASE_URL en .env
```

**2. Dark Mode no funciona**
```bash
# Verificar que @custom-variant esté en index.css
# Comprobar que la clase .dark se aplique al html
```

**3. Rutas no funcionan**
```bash
# Verificar que el servidor soporte HTML5 history API
# En producción, configurar redirects a index.html
```

**4. TypeScript Errors**
```bash
# Limpiar cache de TypeScript
rm -rf node_modules/.cache
npm install
```

## 🤝 Contribución

1. Fork el proyecto
2. Crear branch para feature (`git checkout -b feature/amazing-feature`)
3. Commit cambios (`git commit -m 'Add amazing feature'`)
4. Push al branch (`git push origin feature/amazing-feature`)
5. Abrir Pull Request

## 📄 Licencia

Este proyecto está bajo la Licencia MIT. Ver `LICENSE` para más detalles.

## 👥 Autor

**Jhorman Orozco**
- GitHub: [@jhormanorozco](https://github.com/jhormanorozco)

---

<div align="center">

**🏠 Real Estate Frontend** - Construido con ❤️ y las mejores prácticas de React

</div>